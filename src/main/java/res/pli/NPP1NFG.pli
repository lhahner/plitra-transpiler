orgbachelorpli: PACKAGE;
	
	DCL test_1 DECIMAL(31);
	DCL  n DECIMAL(30);
	
	fibonacci: PROC(n);
		IF (n = 0) THEN;
			RETURN 0;
		ELSE
			IF (n = 1) THEN;
				RETURN 1;
			END;
			n = n - 1;	
			CALL fibonacci;
			RETURN n;
		END
	END fibonacci;	
END orgbachelorpli;