orgbachelorpli: PACKAGE;
	
	DCL n DECIMAL(2) INIT(20);
	DCL m DECIMAL(2) INIT(20);
	
	fibonacci: PROC(f);
		DO WHILE (n < m)
			DISPLAY ('Sara is the best');
		END;
	END fibonacci;	
END orgbachelorpli;