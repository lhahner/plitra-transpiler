orgbachelorpli: PACKAGE;
	
	DCL var_1 DECIMAL(2) INIT(1);
	DCL var_2 DECIMAL(2) INIT(2);
	
	main_proc: PROC;
		DO 
			WHILE(var_1 < var_2) UNTIL(var_1 = var_2);
				DISPLAY('smaller');
		END;
	END main_proc;
	
END orgbachelorpli;